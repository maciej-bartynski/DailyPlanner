import {StyleSheet} from 'react-native';

const ContentWrapperStyles = StyleSheet.create({
  card__wrapper: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    height: '100%',
    flex: 1,
  },
});

export default ContentWrapperStyles;