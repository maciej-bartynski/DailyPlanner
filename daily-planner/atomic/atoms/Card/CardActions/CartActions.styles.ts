import {StyleSheet} from 'react-native';

const CardActionsStyles = StyleSheet.create({
  card__actions: {
    justifyContent: 'space-between',
    alignItems: 'center',
    flexDirection: 'row',
    width: '100%',
  },
});

export default CardActionsStyles;
