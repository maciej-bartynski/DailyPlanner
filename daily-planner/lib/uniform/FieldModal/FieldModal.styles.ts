import {StyleSheet, StyleProp, ViewStyle} from 'react-native';

export interface iFieldModalStyles {
  fieldModal: StyleProp<ViewStyle>;
}

const FieldModalStyles: iFieldModalStyles = StyleSheet.create({
  fieldModal: {},
});

export default FieldModalStyles;
